apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: pranav-application
spec:
  selector:
    app: frontend
  ports:
    - port: 3000
      protocol: TCP
    
  type: ClusterIP
  selector:
    role: frontend
