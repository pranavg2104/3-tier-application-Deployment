apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: pranav-application
spec:
  selector:
    app: frontend
  ports:
    - port: 8080
      protocol: TCP
    
  type: ClusterIP
  selector:
    role: backend